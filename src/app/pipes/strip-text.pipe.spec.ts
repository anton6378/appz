import { StripTextPipe } from './strip-text.pipe';

describe('StripTextPipe', () => {
  it('create an instance', () => {
    const pipe = new StripTextPipe();
    expect(pipe).toBeTruthy();
  });
});
